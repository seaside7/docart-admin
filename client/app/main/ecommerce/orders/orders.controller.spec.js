'use strict';

describe('Component: OrdersComponent', function () {

  // load the controller's module
  beforeEach(module('app.orders'));

  it('should ...', function () {
    expect(1).to.equal(1);
  });
});
