'use strict';

describe('Controller: UsersController', function () {

  // load the controller's module
  beforeEach(module('app.users'));


  it('should ...', function () {
    expect(1).to.equal(1);
  });
});
